h2 Currently Online
div#user-list
  ul
    - if (users.length)
        - users.forEach(function(user){
            - if (user=='Sudar') {
                li.admin= user   
            - } else {
                li= user
            - }
        - })
